version: '3.8'

services:
  web:
    build: .
    container_name: studymate_ai_app
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - JWT_SECRET=studymate-production-secret-key-998877
    restart: always
